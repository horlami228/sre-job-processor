global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "job-processor"
    static_configs:
      - targets: ["localhost:8080"]

  - job_name: "job-worker"
    static_configs:
      - targets: ["localhost:8001"]
